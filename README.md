# phenix-packages

`phenix-packages` is intentionally empty for now.

It reserves the layer-4 package aggregation point. Real aggregation inputs and
outputs should be added here once the package set is ready to be composed.
